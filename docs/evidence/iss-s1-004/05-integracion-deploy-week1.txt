# Integracion con el orquestador scripts/deploy-week1.sh (ISS-S1-004).
#
# deploy-week1.sh registra sus pasos en el arreglo fijo PROVISION_STEPS (NO usa
# glob). "provision-app-service.sh  # ISS-S1-004" ya estaba previsto en la
# posicion 2 desde el cierre de ISS-S1-002/003. Por eso NO se modifico
# deploy-week1.sh, validate-week1.sh ni destroy-week1.sh en esta issue.

$ grep -n "PROVISION_STEPS" -A6 scripts/deploy-week1.sh
22:PROVISION_STEPS=(
23-  "provision-storage.sh"            # ISS-S1-003
24-  "provision-app-service.sh"        # ISS-S1-004
25-  "provision-network.sh"            # ISS-S1-005
26-  "configure-private-endpoints.sh"  # ISS-S1-005
27-)
28-
--
62:  for step in "${PROVISION_STEPS[@]}"; do
63-    if [ -f "$SCRIPT_DIR/$step" ]; then
64-      log_info "Ejecutando paso: $step"; bash "$SCRIPT_DIR/$step"
65-    else
66-      log_warn "Paso pendiente (issue futura): $step"
67-    fi
68-  done

# El paso solo se ejecuta si el archivo existe; ahora existe y se ejecutara:
$ grep -n "provision-app-service.sh" scripts/deploy-week1.sh
24:  "provision-app-service.sh"        # ISS-S1-004

# La validacion de SKU/region compatible con slots ya vive en deploy-week1.sh
# y validate-week1.sh (via `az appservice list-locations --sku`), y ademas
# provision-app-service.sh la revalida de forma autonoma (assert_sku_supports_slots).

$ bash -n scripts/deploy-week1.sh scripts/validate-week1.sh scripts/destroy-week1.sh
OK sintaxis (sin cambios): scripts/deploy-week1.sh
OK sintaxis (sin cambios): scripts/validate-week1.sh
OK sintaxis (sin cambios): scripts/destroy-week1.sh
