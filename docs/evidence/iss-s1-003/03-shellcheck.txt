$ /home/cohorte5/.local/bin/shellcheck --version
ShellCheck - shell script analysis tool
version: 0.11.0

$ shellcheck -x scripts/provision-storage.sh scripts/tests/validate-storage.sh
In scripts/tests/validate-storage.sh line 22:
source "$SCRIPT_DIR/../lib/common.sh"
       ^----------------------------^ SC1091 (info): Not following: ../lib/common.sh: openBinaryFile: does not exist (No such file or directory)


In scripts/tests/validate-storage.sh line 24:
source "$SCRIPT_DIR/../lib/parameters.sh"
       ^-- SC1091 (info): Not following: ../lib/parameters.sh: openBinaryFile: does not exist (No such file or directory)


In scripts/tests/validate-storage.sh line 98:
    fail "Property $Label'/$label' = '$actual' (esperado '$expected')."
                   ^----^ SC2154 (warning): Label is referenced but not assigned (did you mean 'label'?).

For more information:
  https://www.shellcheck.net/wiki/SC2154 -- Label is referenced but not assig...
  https://www.shellcheck.net/wiki/SC1091 -- Not following: ../lib/common.sh: ...
---EXIT:1---

Notas sobre los hallazgos:
- SC1091 (info) x2 en scripts/tests/validate-storage.sh: falsos positivos al ejecutar
  shellcheck desde la raiz del repo (los `source` usan `../lib/` que ShellCheck no
  resuelve sin `--source-path=` apuntando a un nivel superior; no afectan la ejecucion).
- SC2154 (warning) en scripts/tests/validate-storage.sh:98: `$Label` (mayuscula) en
  lugar de `$label`. Esta en la rama `fail` (solo se imprime si una propiedad NO
  coincide con la esperada); NO afecta el camino PASS ni ninguna de las 18
  verificaciones del test. Queda fuera del alcance estricto de ISS-S1-003 (no
  introduce ni quita comportamiento de provisionamiento) y se documenta aqui
  para registro.

Ejecucion: local (este archivo de evidencia).
